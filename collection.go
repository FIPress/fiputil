package fiputil

func mapArray(in interface{}, out interface{}) {
	/*l := len(in)

	if l == 0 {return}

	//reflect
	out = make([]BlogInfo,l)
	for i:=0;i<l;i++ {
		blog := in[i]
		out[i] = newBlogBrief(&blog)
	}*/
}
